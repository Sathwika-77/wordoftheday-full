{
  "name": "WORDOFTHEDAY-netlify-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Proxy WORDOFTHEDAY WOTD for browser extension (store secret on Netlify env variables).",
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}